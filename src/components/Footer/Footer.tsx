import { StyledFooter, StyledFooterSection } from '@components/Footer/Footer.styles';

export const Footer = () => {
    return (
        <StyledFooter>
            <StyledFooterSection>
                <span>©MichaelHur.dev. Built with React</span>
            </StyledFooterSection>
        </StyledFooter>
    );
};
